{% extends 'layouts/default.njk' %}

{% block content %}
  <article>
    <header class="o-content">
      <time class="u-eyebrow">{{ page.date | date_formatted }}</time>
      <h1>{{ title }}</h1>
      {% if link %}
        <p class="u-margin-top-025">
          {% Link link, "Visit link" %}
        </p>
      {% endif %}
      {% if intro %}
        <p class="u-lead">{{ intro }}</p>
      {% endif %}
    </header>
    <div class="o-content o-content--post">
      {{ content | safe }}
    </div>
  </article>
{% endblock %}
