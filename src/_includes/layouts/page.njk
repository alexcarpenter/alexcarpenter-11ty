{% extends 'layouts/default.njk' %}

{% block header %}
  <header class="u-margin-bottom-3">
    <h1 class="u-font-size-xxl u-heading-decorate">{{ title }}</h1>
    {% if summary %}
    <p class="u-margin-top-05 u-font-size-md">{{ summary | markdownify_inline | safe }}</p>
    {% endif %}
  </header>
{% endblock %}

{% block content %}{% endblock %}
