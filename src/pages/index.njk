---
title: "UI Engineer"
summary: "Currently interested in CSS architecture, functional programming, React, design systems, and state machines."
permalink: /index.html
---
{% extends "layouts/default.njk" %}

{% block content %}
  <header class="u-linear u-margin-bottom-3">
    <h1 class="u-font-size-xxl u-heading-decorate">UI Engineer</h1>
    <div class="c-avatar" style="--aspect-ratio: 1/1;">
      <img src="/assets/images/photo-300.jpg" alt="Photo of Alex Carpenter">
    </div>
    <p class="u-font-size-md u-line-height-tighten u-margin-top-05">Currently interested in CSS architecture, functional programming, React, design systems, and state machines.</p>
    <p>I am a self taught developer that works best when floating in between design and engineering teams. I enjoy sharing what I know via short intructional screencasts and the occasional written post.</p>
    {# <div><a href="" class="c-button c-button--muted">Download resume</a></div> #}
  </header>

  <section class="u-linear">
    <h2>Popular screencasts</h2>
    <ul class="c-listing">
      {% for post in collections.screencasts | favorites(4) %}
      <li class="c-listing__item">
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </li>
      {% endfor %}
    </ul>
    <div>{% Link '/screencasts', 'View more' %}</div>
  </section>

  <section class="u-linear">
    <h2>Recent posts</h2>
    <ul class="c-listing">
      {% for post in collections.posts | head(4) %}
      <li class="c-listing__item">
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </li>
      {% endfor %}
    </ul>
    <div>{% Link '/posts', 'View more' %}</div>
  </section>

  <section class="u-linear">
    <h2>Projects</h2>
    <ul class="c-listing">
      {% for project in projects | head(4) %}
      <li class="c-listing__item">
        {% Link project.url, project.title, true %}
        <br>
        <span class="u-color-gray">{{ project.description }}</span>
      </li>
      {% endfor %}
    </ul>
    <div>{% Link 'https://github.com/alexcarpenter', 'View more' %}</div>
  </section>
{% endblock %}
